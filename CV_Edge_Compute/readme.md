This is the edge device for CV computation on the Jetson.

It contains a python virtual environment that needs to be ran first:
/myenv

Run the CV detection python file using python directly and not ROS.
